import React from 'react'

function PageTitle({title}) {
  return (
    <section id="section_page_title">
        <h2 className="page_title">{title}</h2>
    </section>
  )
}

export default PageTitle
